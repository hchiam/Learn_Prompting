export const metadata = {
  sidebar_position: 7,
  title: "🟢 Qu'y a-t-il dans un Prompt ?",
};

# 🟢 Qu'y a-t-il dans un Prompt ?

Lors de la création de prompts pour les modèles d'apprentissage du langage (LLM), plusieurs facteurs sont à prendre en compte. Le format et l'espace d'étiquettes[^a] jouent tous deux des rôles cruciaux dans l'efficacité du prompt.

## L'Importance du Format

Le format des <Term term="exemplars">exemples</Term> dans un prompt est crucial. Il indique au LLM comment structurer sa réponse. Par exemple, si les exemples utilisent des mots en majuscules comme réponses, le LLM fera de même, même si les réponses fournies sont incorrectes.

Considérez l'exemple suivant :

```text
Combien font 2+2 ?
CINQUANTE
Combien font 20+5 ?
QUARANTE-TROIS
Combien font 12+9 ?
VINGT-ET-UN
```

Malgré les réponses incorrectes, le LLM formate correctement sa réponse en lettres majuscules (@min2022rethinking).

## Vérité Fondamentale : Pas Aussi Importante Qu'On Pourrait Le Penser

Fait intéressant, les réponses réelles ou la 'vérité fondamentale' dans les exemples ne sont pas aussi importantes qu'on pourrait le penser. La recherche montre que fournir des <Term term="labels">étiquettes</Term> aléatoires dans les exemples (comme on le voit dans l'exemple ci-dessus) a peu d'impact sur la performance (@min2022rethinking). Cela signifie que le LLM peut toujours générer une réponse correcte même si les exemples contiennent des informations incorrectes.

## Le Rôle de l'Espace d'Étiquettes

Bien que la vérité fondamentale puisse ne pas être cruciale, l'espace d'étiquettes l'est. L'espace d'étiquettes fait référence à la liste des étiquettes possibles pour une tâche donnée. Par exemple, dans une tâche de classification, l'espace d'étiquettes pourrait inclure "positif" et "négatif".

Fournir des étiquettes aléatoires de l'espace d'étiquettes dans les exemples peut aider le LLM à mieux comprendre l'espace d'étiquettes, conduisant à de meilleurs résultats. De plus, il est important de représenter précisément la distribution de l'espace d'étiquettes dans les exemples. Au lieu d'échantillonner uniformément à partir de l'espace d'étiquettes, il est préférable d'échantillonner selon la distribution réelle des étiquettes (@min2022rethinking). Par exemple, si vous avez un ensemble de données d'avis de restaurants et que 60\% d'entre eux sont positifs, votre prompt devrait contenir un ratio de 3:2 de prompts positifs/négatifs.

## Conseils Supplémentaires

Lors de la création de prompts, utiliser entre 4 et 8 exemples tend à donner de bons résultats (@min2022rethinking). Cependant, il peut souvent être bénéfique d'inclure autant d'exemples que possible.

En conclusion, comprendre l'importance du format, de la vérité fondamentale et de l'espace d'étiquettes peut grandement améliorer l'efficacité de vos prompts.

[^a]: Voir la [référence du vocabulaire](https://learnprompting.org/docs/vocabulary#labels) pour plus d'informations.
