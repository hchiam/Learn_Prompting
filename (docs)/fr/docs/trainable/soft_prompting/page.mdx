export const metadata = { sidebar_position: 1, title: "🔴 Prompts Doux" };

# 🔴 Prompts Doux

Le réglage de prompt (@lester2021power), une alternative au réglage fin de modèle (@khashabi2021prompt), gèle les poids du modèle et met à jour les paramètres d'un prompt. Le prompt résultant est un 'prompt doux'.

<div style={{ textAlign: "center" }}>
  <Image
    src="/docs/assets/trainable/prompt_tuning.webp"
    width={1074}
    height={556}
    style={{ width: "500px", margin: "auto" }}
  />
</div>

<div style={{ textAlign: "center" }}>
  Réglage de Modèle vs Réglage de Prompt (Lester et al.)
</div>

L'image ci-dessus contraste le réglage de modèle avec le réglage de prompt.
Dans le réglage de modèle, vous affinez le même modèle sur différentes tâches. Cela vous donne
quelques modèles différents, avec lesquels vous ne pouvez pas nécessairement traiter facilement les entrées par lots.

D'autre part, le réglage de prompt vous permet d'utiliser le même modèle pour toutes les tâches. Vous
avez juste besoin d'ajouter les prompts appropriés au moment de l'inférence, ce qui facilite le traitement par lots
à travers différentes tâches. C'est à peu près le même avantage que le prompting régulier
possède. De plus, les prompts doux entraînés pour un seul modèle à travers
plusieurs tâches auront souvent la même longueur de tokens.

## Comment ça fonctionne

Pour comprendre la logique de base derrière les prompts doux, réfléchissons à comment **l'inférence du modèle** fonctionne
sur un prompt donné : `Combien fait 2+2 ?`.

1. Il pourrait être tokenisé comme `Combien, fait, 2, +, 2, ?`.

2. Ensuite, chaque token sera converti en un vecteur de valeurs.

3. Ces vecteurs de valeurs peuvent être considérés comme des paramètres du modèle. Le modèle peut être davantage
   entraîné, en ajustant uniquement les poids de ces prompts.

Notez que dès que nous commençons à mettre à jour ces poids, les vecteurs des tokens ne
correspondent plus aux embeddings réels du vocabulaire.

# Résultats

Le réglage de prompt fonctionne mieux avec des modèles plus grands. Les modèles plus grands nécessitent également moins de
tokens de prompt doux. Quoi qu'il en soit, plus de 20 tokens n'apporte pas de gains de performance significatifs.
