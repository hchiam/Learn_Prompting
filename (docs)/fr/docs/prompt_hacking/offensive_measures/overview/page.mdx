export const metadata = {
  sidebar_position: 0,
  title: "🟢 Vue d'ensemble",
};

# 🟢 Vue d'ensemble

Cette section couvre les techniques offensives pour le piratage de prompts. Nous discutons de 4 techniques principales :

1. Stratégies d'obfuscation qui tentent de cacher des tokens malveillants (par exemple, en utilisant des synonymes, des fautes de frappe, l'encodage Base64).
2. Fractionnement de charge utile, dans lequel des parties d'un prompt malveillant sont divisées en parties non malveillantes.
3. L'attaque de dictionnaire défini, qui contourne la défense en sandwich
4. La virtualisation, qui tente de pousser un chatbot dans un état où il est plus susceptible de générer une sortie malveillante. Cela prend souvent la forme d'émulation d'une autre tâche.

Ensuite, nous discutons de 2 grandes classes d'injection de prompt :

1. L'injection indirecte, qui utilise des sources de données tierces comme les recherches web ou les appels API.
2. L'injection récursive, qui peut pirater à travers plusieurs couches d'évaluation de modèle de langage
