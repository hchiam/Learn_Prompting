export const metadata = { sidebar_position: 30, title: "🟢 Post-Prompting" };

# 🟢 Post-Prompting

La défense par post-prompting (@christoph2022talking) consiste simplement à placer
l'entrée utilisateur _avant_ le prompt. Prenons ce prompt comme exemple :

```text
Traduisez ce qui suit en français : {{user_input}}
```

Il peut être amélioré avec le post-prompting :

```text
{{user_input}}

Traduisez le texte ci-dessus en français.
```

Cela peut aider car `ignorez les instructions ci-dessus...` ne fonctionne pas aussi bien. Même si un utilisateur pourrait dire `ignorez les instructions ci-dessous...` à la place, les LLM suivent souvent la dernière instruction qu'ils voient.
