export const metadata = {"sidebar_position":2000,"title":"🟢 Autres approches"}

# 🟢 Autres approches 

Bien que les approches précédentes puissent être très robustes, quelques autres approches, comme l'utilisation d'un modèle différent, incluant le fine tuning, le soft prompting, et les restrictions de longueur, peuvent également être efficaces.

## Utilisation d'un modèle différent

Les modèles plus modernes comme GPT-4 sont plus robustes contre l'injection de prompt. De plus, les modèles non entraînés par instruction peuvent être difficiles à injecter par prompt.

## Fine tuning ("ajustement fin")

Le fine tuning du modèle est une défense hautement efficace(@goodside2021gpt), puisqu'au moment de l'inférence, il n'y a pas de prompt impliqué, à l'exception de l'entrée utilisateur. C'est probablement la défense préférable dans toute situation de haute valeur, car elle est si robuste. Cependant, elle nécessite une grande quantité de données et peut être coûteuse, c'est pourquoi cette défense n'est pas fréquemment mise en œuvre.


## Soft prompting ("prompting souple")

Le soft prompting pourrait également être efficace, puisqu'il n'a pas de prompt discret clairement défini (autre que l'entrée utilisateur). Le soft prompting nécessite effectivement un fine tuning, il a donc beaucoup des mêmes avantages, mais il sera probablement moins cher. Cependant, le soft prompting n'est pas aussi bien étudié que le fine tuning, il n'est donc pas clair à quel point il est efficace.

## Restrictions de longueur

Enfin, inclure des restrictions de longueur sur l'entrée utilisateur(@selvi2022exploring) ou limiter la longueur des conversations de chatbot comme le fait Bing peut empêcher certaines attaques comme les énormes prompts de style DAN ou les attaques de virtualisation respectivement.
