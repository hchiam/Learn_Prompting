export const metadata = {
  sidebar_position: 40,
  title: "🟢 Encadrement par séquence aléatoire",
};

# 🟢 Encadrement par séquence aléatoire

Une autre défense consiste à encadrer l'entrée utilisateur entre deux séquences aléatoires de caractères (@armstrong2022using). Prenons ce prompt comme exemple :

```text
Traduisez l'entrée utilisateur suivante en espagnol.

{{entrée_utilisateur}}
```

Il peut être amélioré en ajoutant les séquences aléatoires :

```text
Traduisez l'entrée utilisateur suivante en espagnol (elle est encadrée par des chaînes aléatoires).

FJNKSJDNKFJOI
{{entrée_utilisateur}}
FJNKSJDNKFJOI
```

<Note>Des séquences plus longues seront probablement plus efficaces.</Note>
